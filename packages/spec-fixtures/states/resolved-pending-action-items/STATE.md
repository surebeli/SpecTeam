State: divergence resolved by consensus, but one collaborator still has a pending source update.
Invariants: validator-compatible resolved entry, decision file present, and pending action-item status retained.
