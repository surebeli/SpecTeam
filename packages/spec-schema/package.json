{
  "name": "@specteam/schema",
  "version": "0.1.0",
  "private": true,
  "description": "Machine-readable schemas and validator for the SpecTeam .spec/ protocol",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test dist-test/test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "^5.1.3"
  }
}
